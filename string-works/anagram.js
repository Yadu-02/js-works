function isAngram(word1,word2)
{

isAngram=true;

if(word1.length!=word2.length){

    isAngram
}

for(let ch of word1)
{
    if(word2[ch]==-1)
    {
        isAngram=false;

        break;
    }
}

}
console.log();

